/* tslint:disable:object-literal-sort-keys max-line-length */
import config from 'ember-get-config';

const {
    OSF: {
        pageName,
        longBrand,
    },
    signUpPolicy: {
        privacyPolicyLink,
        cookiesLink,
    },
    navbar: {
        useSearch,
    },
} = config;

export default {};
