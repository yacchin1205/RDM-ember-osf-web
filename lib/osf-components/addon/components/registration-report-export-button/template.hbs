<BsModal
    data-test-registration-report-modal
    local-class='Modal'
    @open={{this.dialogOpen}}
    @onSubmit={{action this.submit}}
    @onHidden={{action this.hideDialog}}
    as |modal|
>
    <modal.body data-analytics-scope='RegistrationReport modal'>
        <div>
            <h4 local-class='Modal__title'>{{ t 'metadata.registration-card.export-dialog.title' }}</h4>
        </div>
        <div local-class='Export__description'>
            {{t 'metadata.registration-card.export-dialog.description'}}
        </div>
        <div class='form-group'>
            <select id='registration-report-format-selection'>
                {{#each this.metadataFormats as |format|}}
                    <option
                        value='{{format.id}}'>
                        {{format.name}}
                    </option>
                {{/each}}
                {{#each this.metadataDestinations as |destination|}}
                    <option
                        value='{{destination.id}}'>
                        {{t (concat 'metadata.registration-card.export-dialog.addons.' destination.provider) folder=destination.folder}}
                    </option>
                {{/each}}
            </select>
        </div>
    </modal.body>
    <modal.footer data-analytics-scope='RegistrationReport modal footer'>
        <OsfButton
            data-test-registration-report-cancel
            data-analytics-name='Cancel'
            @onClick={{action modal.close}}
        >
            {{t 'general.cancel'}}
        </OsfButton>
        <OsfButton
            data-test-registration-report-submit
            data-analytics-name='Submit'
            @disabled={{this.isUploading}}
            @type='success'
            @onClick={{action modal.submit}}
        >
            {{#if this.isUploading}}
                {{t 'metadata.registration-card.exporting'}}
            {{else}}
                {{t 'metadata.registration-card.export'}}
            {{/if}}
        </OsfButton>
    </modal.footer>
</BsModal>

{{#if (gt this.workflowRegistrations.length 0)}}
    <div class="btn-group btn-group-sm" local-class="ExportButton__dropdown">
        <OsfButton
            data-test-registration-card-export
            data-analytics-name='Export'
            @type='success'
            @disabled={{this.isDisabled}}
            @onClick={{action this.primaryAction}}
        >
            {{this.buttonLabel}}
        </OsfButton>
        <BsDropdown as |dd|>
            <dd.button @type="success" @disabled={{this.isDisabled}} class="btn-sm dropdown-toggle">
                <span class="caret"></span>
                <span class="sr-only">{{t 'metadata.registration-card.toggle-actions'}}</span>
            </dd.button>
            <dd.menu @align='right' as |menu|>
                <menu.item role='menuitem'>
                    <a href="#" {{on "click" this.selectExport}}>{{t 'metadata.registration-card.export'}}</a>
                </menu.item>
                <menu.divider />
                <li class="dropdown-header">{{t 'metadata.registration-card.start-workflow-header'}}</li>
                {{#each this.workflowRegistrations as |workflow|}}
                    <menu.item role='menuitem'>
                        <a href="#" {{on "click" (fn this.selectWorkflow workflow.id)}}>{{workflow.displayLabel}}</a>
                    </menu.item>
                {{/each}}
            </dd.menu>
        </BsDropdown>
    </div>
{{else}}
    <OsfButton
        data-test-registration-card-export
        data-analytics-name='Export'
        class={{this.buttonClass}}
        @type='success'
        @disabled={{this.isDisabled}}
        @onClick={{action this.export}}
    >
        {{t 'metadata.registration-card.export'}}
    </OsfButton>
{{/if}}
